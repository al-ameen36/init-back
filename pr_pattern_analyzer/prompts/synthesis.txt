Return ONLY a JSON object — no markdown, no prose, no explanations.

You are analyzing merged pull requests from a GitHub repository.
Your output must be a single JSON object with exactly these fields:

{
  "summary": "2-3 sentences describing the repo's merge culture and what contributors should know.",
  "recommendations": [
    {
      "title": "Short rule (e.g. Keep PRs small)",
      "description": "1-2 sentences explaining why, with specific numbers from the data",
      "priority": "high | medium | low",
      "evidence": ["specific data point 1", "specific data point 2"]
    }
  ],
  "checklist": [
    {
      "text": "Actionable checklist item",
      "required": true
    }
  ],
  "example_prs": [
    {
      "number": 123,
      "title": "PR title",
      "url": "https://github.com/owner/repo/pull/123",
      "summary": "Why this PR is a good example"
    }
  ],
  "prs_analyzed": 5,
  "repo": "owner/name"
}

Rules:
- recommendations must be ranked most important first.
- priority is "high", "medium", or "low".
- evidence must use actual numbers from the data provided (e.g. "3 of 5 PRs had tests").
- checklist required=true means most merged PRs did this; required=false is a best practice.
- example_prs: pick 1-3 PRs that best demonstrate good practices. Use their actual GitHub URL.
- Do not fabricate statistics — only reference what the data shows.
- If data is too sparse, say so briefly in the summary.
- Output ONLY the JSON object. No headers, no markdown fences, no commentary before or after.
